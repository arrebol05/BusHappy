# BusHappy Backend Environment Configuration

# Environment mode: 'sandbox' or 'production'
# Default: sandbox (safe for testing and editing)
# Production: read-only mode, prevents accidental data modifications
BUSHAPPY_ENV=sandbox
